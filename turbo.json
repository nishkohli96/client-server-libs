{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "lint": {
      "dependsOn": []
    },
    "lib:build": {
      "dependsOn": []
    },
    "build": {
      "dependsOn": ["lib:build"],
      "outputs": ["build/**", "dist/**", ".next/**"]
    }
  }
}
