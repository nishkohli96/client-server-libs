export const FileRouteConfig = Object.freeze({
  /**
   * This folder will be outside the src directory, so that
   * the build can also access the media at this path.
   */
  uploadFolder: 'uploads'
});
