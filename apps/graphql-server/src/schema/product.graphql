type Product {
  id: ID!
  name: String!
  description: String!
  price: Float!
  categoryId: ID!
}

input ProductInput {
  name: String!
  description: String
  price: Float!
  categoryId: ID!
}

type Query {
  getProducts: [Product!]!
  getProductById(productId: ID!): Product
}

type Mutation {
  createProduct(input: ProductInput!): Product!
}

